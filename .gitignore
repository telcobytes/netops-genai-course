# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/

# Secrets — never commit an API key
.env
*.key

# OS / editor
.DS_Store
.vscode/
.idea/

# Diagnostics — not course material, must not ship to students (see CLAUDE.md)
verify_all.py
diag_toolcall.py
diag_multiturn.py
measure_nondeterminism.py

# Run artifact, not a fixture. Committed, it carries no case_id, so run_eval's
# load_trace() matched every entry to every case and graded one stale trace as
# evidence for all three eval cases.
module10-eval/trace_log.jsonl

# Maintainer notes for Claude Code. Students clone this repo (notebook cell 2 does
# git clone + git pull), and this file catalogues defects the course has shipped.
CLAUDE.md

# Claude Code's own config and skills — review tooling for whoever maintains the
# course, not something a student should find in their clone. Skills live in
# ~/.claude/skills/ so they work across projects.
.claude/

# Checkpoint 1 writes the student's own runbook here. It is their answer, not
# course material, and it must never reach data/knowledge_base/ (see that
# checkpoint's README).
checkpoints/01_rag/kb/
